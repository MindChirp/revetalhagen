"use client";

export default function CalendarWrapper() {
  return (
    <div className="w-fit mx-auto">
      {/* <Calendar
        mode="range"
        selected={data}
        onSelect={(range) => setData(range)}
      /> */}
    </div>
  );
}
